<h2>Title: <%= @job.title %></h2>

<p>Description: <%= @job.description %></p>

<p>Level of Interest: <%= @job.level_of_interest %></p>

<p>City: <%= @job.city %></p>

<p>Company: <%= link_to @job.company.name, company_path(@job.company) %></p>

<p>Category: <%= link_to @job.category.title, category_path(@job.category) %></p>

<h3>Comments <%= @job.comments.count %></h3>
<%= render partial: 'jobs/comment', collection: @job.comments.reverse %>
<%= render partial: 'comments/form' %>

<p><%= link_to 'Edit Job', edit_company_job_path(@job.company, @job)%></p>
<p><%= link_to 'Delete',  company_job_path(@job.company, @job), method: :delete%></p>
<p><%= link_to "<< Back to all jobs @ #{@job.company.name}", company_jobs_path(@job.company) %>
